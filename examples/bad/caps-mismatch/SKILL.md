---
name: Caps_Mismatch
description: Use when you want to see how Signalman reports a name that is not lowercase-hyphenated and does not match its own directory name.
---

# Caps mismatch

The body is fine; the frontmatter `name` is the problem. It uses capitals and an
underscore, and it does not match the `caps-mismatch` directory.
